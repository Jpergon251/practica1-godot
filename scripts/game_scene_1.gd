extends Node2D

@onready var spawn_point: Node2D = $Portal1/SpawnPoint

## Called when the node enters the scene tree for the first time.
#func _ready() -> void:
	#game_manager._instanciar_jugador(spawn_point.global_position)
